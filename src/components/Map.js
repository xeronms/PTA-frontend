import React from 'react';
import Mapbox from './MapboxEx';
import './Map.css';

const Map = () => {
  return (
    <div class="mapContainer">
      <Mapbox></Mapbox>
    </div>
  );
};

export default Map;
